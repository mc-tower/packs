{
  "scripts": {
    "build": "scripts/build/run.sh",
    "format": "prettier --write --plugin-search-dir=. './{resourcepacks,scripts}/**/*.{js,mjs,json}'"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "sanitize-html": "^2.7.0",
    "showdown": "^2.0.0-alpha"
  }
}
